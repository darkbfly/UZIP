---
tags:
  - dev
---

# 开发指南

## 环境

| 项 | 建议 |
|----|------|
| IDE | Visual Studio（打开 `src/UZIP2/UZIP2.sln`） |
| 目标框架 | .NET Framework **4.5** |
| JDK/Maven | 无关（本仓库为 C# WPF） |

本机若需命令行构建，用对应 VS Developer Prompt / MSBuild 指向该 `.csproj`。

## GitHub Actions 发版

工作流：`.github/workflows/release.yml`

| 触发 | 做法 |
|------|------|
| 打 tag | `git tag v1.0.0` → `git push origin v1.0.0` |
| 手动 | GitHub → Actions → **Release** → Run workflow → 填版本号 |

产物：`UZIP2-v*-win.zip`（含 `UZIP2.exe`、`UZIP2.exe.config`、`7-Zip\7z.exe`），自动挂到对应 GitHub Release。

本地等价打包：

```powershell
msbuild src\UZIP2\UZIP2\UZIP2.csproj /t:Rebuild /p:Configuration=Release /p:Platform=AnyCPU
```

## 本地运行

1. 打开 `src/UZIP2/UZIP2.sln`
2. 配置 `Debug`，启动项目 `UZIP2`
3. 确认输出目录存在可用的 `7-Zip\7z.exe`（或设置自定义路径）
4. 首次运行会在 exe 旁使用 `Config\` 下配置文件

## 命令行

解析：`CliArgs.cs`；启动：`App.Application_Startup`（无 `StartupUri`）。

```text
UZIP2.exe extract <档案...> [-o 目录] [-p 密码] [-q]
UZIP2.exe compress <文件/目录...> [-o 目录] [-p 密码] [-q]
UZIP2.exe auto <路径...> [-o 目录] [-p 密码] [-q]
UZIP2.exe watch <文件夹> [-o 目录] [-p 密码] [-q]
UZIP2.exe <路径...>          # 等价 auto
UZIP2.exe -h | --help
```

| 选项 | 含义 |
|------|------|
| `-o` | 输出目录 |
| `-p` | 本次额外尝试的密码（仅内存，不写密码纸配置） |
| `-q` | 隐藏主窗；单次任务结束后退出；`watch` 时进托盘常驻监听 |

`watch` **只解压**监听到的压缩包（普通文件忽略）。路径与 `-o` 仅对本进程有效，不改设置页里的监听配置。

示例：

```text
UZIP2.exe watch D:\inbox -o D:\out -q
```

默认弹主窗并自动开干；CLI 单次任务不弹「浏览路径」窗（无 `-o` 时用文件所在目录）。

## 多开

无单实例锁。软隔离：窗口位置按其它 `UZIP2` 进程数错位 +30；热键注册失败则跳过；托盘文案 `UZip #xxxx`。仍共用同一 `Config\`。

## Debug 模式

- `ShowDebug`：仅改配置文件开启后，设置页才显示 Debug 开关
- `DebugMode`：开发者模式，可打开 `DebugWindow`

## 改代码前

按 [[首页]] 场景表先查知识库对应模块页，再下钻源码。

## 改代码后

按「知识库同步」规则回写 `doc/obsidian/`：模块页、索引、开发指南中与行为相关的描述。

## 模板

新建模块笔记可用：[[模块笔记模板]]
