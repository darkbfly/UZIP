---
tags:
  - module
  - moc
---

# 模块索引

| 模块 | 笔记 | 主要代码 |
|------|------|----------|
| 主窗口 / 拖拽流程 | [[主窗口]] | `MainWindow.xaml(.cs)` |
| 设置与配置 | [[设置与配置]] | `Setting.cs`, `Config.cs` |
| 密码管理 | [[密码管理]] | `Setting.cs` → `Password`, `KeyBoard.xaml` |
| 7-Zip 调用 | [[7z调用]] | `Tools.cs`, `MainWindow.xaml.cs` |
| 辅助窗口 | （见下表） | `SelectOut*`, `ResultWindow`, `DebugWindow`, `YesNo` |

## 辅助窗口

| 窗口 | 文件 | 用途 |
|------|------|------|
| 选择输出 | `SelectOut.xaml`, `SelectOut2.xaml` | 解压/压缩输出路径选择 |
| 结果 | `ResultWindow.xaml` | 独立显示结果（`ResultWindow` 配置） |
| 屏幕键盘 | `KeyBoard.xaml` | 密码输入辅助 |
| 调试 | `DebugWindow.xaml` | `ShowDebug`/`DebugMode` |
| 确认 | `YesNo.xaml` | 是/否确认 |

## 运行状态机（摘要）

`RunStatus`（`Setting.cs`）：`Normal` → 设置/密码编辑/迷你模式/模式选择/解压中… 各状态限制 UI 操作。细节见 [[主窗口]]、[[设置与配置]]。
